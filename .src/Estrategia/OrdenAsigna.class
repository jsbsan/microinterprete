' Gambas class file

Inherits InterfaceCrearComando
Private comandotmp As New Asigna

Public Sub guard(valor As String) As Boolean

    If valor = ("asigna") Then
        Return True
    Else
        Return False
    Endif

End

Public Sub crearComando(linea As String) As Comando

    Dim palabras As String[]

    palabras = Split(linea, ",") 'caracter separador de datos

    If palabras.count = 0 Then
        Return Null
    Else
        comandotmp = New Asigna
        comandotmp.datos = palabras
        comandotmp.tipo = "interno"
        Return comandotmp
    Endif

End

Public Sub ayuda() As String

    'devuelve lo ayuda sobre el comando

    Return "Asigna valor a una variable previamente dimensionada. Sintaxis: asigna,[nombre_variable],[expresión]\n Ejemplo Uso: asigna,x,10*3 -> x valdria 30 "

End
